// Copyright (c) Microsoft Corporation.
// Licensed under the MIT license.

export type CreateDcrOutputs = {
  configurationId: string;
};

// The const is used to reference the property name in CreateDcrOutputs. When renaming the properties in CreateDcrOutputs, you need to update the const as well.
export const OutputKeys = {
  configurationId: "configurationId",
};
